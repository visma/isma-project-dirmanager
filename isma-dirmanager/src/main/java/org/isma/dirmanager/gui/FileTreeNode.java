package org.isma.dirmanager.gui;

import org.isma.dirmanager.model.FileElement;

public class FileTreeNode extends AbstractFileTreeNode<FileElement> {
    public FileTreeNode(FileElement file) {
        super(file);
    }
}
